<UserControl x:Class="TXTReader.Display.Displayer4"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" AllowDrop="True" Focusable="False">    
    <Canvas x:Name="canvas" ClipToBounds="True"/>
    
    <UserControl.ContextMenu>
        <ContextMenu>
            
            <MenuItem Header="打开(_O)" x:Name="mi_open" Click="mi_open_Click"/>
            <MenuItem Header="关闭(_C)" x:Name="mi_close" Click="mi_close_Click"/>
            <MenuItem Header="重开(_R)" x:Name="mi_reopen" Click="mi_reopen_Click"/>
            <Separator/>
            <MenuItem Header="搜索(_F)" x:Name="mi_find" Command="ApplicationCommands.Find"/>
            <Separator/>
            <MenuItem Header="滚屏(_S)" x:Name="mi_scroll" IsCheckable="True"/>
            <Separator/>
            <MenuItem Header="退出(_X)" x:Name="mi_exit" Click="mi_exit_Click"/>
        </ContextMenu>
    </UserControl.ContextMenu>
</UserControl>
